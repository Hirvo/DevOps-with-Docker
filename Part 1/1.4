1:  sudo docker run --rm -it ubuntu sh -c 'echo "Input website:"; read website; echo "Searching.."; sleep 1; curl http://$website;'
2: on another console tab: sudo docker exec -it 00 bash (id started with 00)
3: apt-get update
4: apt-get install curl
5: on the first tab: "helsinki.fi"

